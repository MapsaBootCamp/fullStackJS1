const sum = (a, b) => {
  return a + b;
};

const divide = (a, b) => {
  return a / b;
};

const subtract = (a, b) => {
  return a - b;
};

const mult = (a, b) => {
  return a * b;
};

export { sum, divide, subtract };
export default mult;
// module.exports = {
//   sum,
//   mult,
// };
